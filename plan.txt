0. LReLU between concat and next layer

1. if result unfavourable - downgrade SOC net

2. if result unfavourable - mimic int-net for OC

3. self.batch_size = 600 - > 300

4. epoch 600 to 1200

5. self.lr = 0.01 - > 0.001


6.
                nn.Linear(4 + self.intermediate_size,3),
                nn.LeakyReLU(),
                nn.Linear(3,1)

                >>

                nn.Linear(4 + self.intermediate_size,1)
